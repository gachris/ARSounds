<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="ARSounds.MauiApp.Controls.HeaderTemplate"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:fontIcons="clr-namespace:ARSounds.UI.FontIcons"
    BackgroundColor="{DynamicResource MainWrapperBackgroundColor}">
    <ContentView.Resources>
        <Style x:Key="HeaderStyle" TargetType="Label">
            <Setter Property="VerticalOptions" Value="Center" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="HeightRequest" Value="46" />
            <Setter Property="VerticalTextAlignment" Value="Center" />
            <Setter Property="TextColor" Value="{DynamicResource BaseTextColor}" />
            <Setter Property="HorizontalOptions" Value="Start" />
        </Style>
    </ContentView.Resources>
    <Grid Margin="0" ColumnSpacing="10">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="46" />
        </Grid.RowDefinitions>

        <Label
            x:Name="HeaderLabel"
            Grid.Column="0"
            Style="{StaticResource HeaderStyle}"
            Text="Header" />

        <Label
            x:Name="HeaderIcon"
            Grid.Column="1"
            FontFamily="IonIcons"
            FontSize="30"
            Style="{StaticResource HeaderStyle}"
            Text="{x:Static fontIcons:IonIcons.IosPeople}"
            TranslationY="-1" />

    </Grid>
</ContentView>
